{
  "scripts": {
    "build": "npx tsc -b",
    "watch": "npx tsc -b -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
